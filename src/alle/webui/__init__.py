"""alle's Web UI: a loopback HTTP control API + static app, served by alled.

Phase 4.1 (app shell + Dashboard). The server runs as a thread inside the daemon
so there is nothing extra to deploy; assets ship in the wheel. See
``server.py`` for the transport/auth model.
"""
